import logging


def collect_twitter(*args, **kwargs) -> list[dict]:
    logging.info(
        "X/Twitter collection requires API access. Historical data may require paid full-archive access. This collector is a placeholder."
    )
    return []
